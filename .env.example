# Example configuration for the social & news monitoring dashboard
# Copy this file to `.env` and fill in your own values. All values are optional;
# the app works with public sources even without this file.

# SQLite database file path
TRACKER_DB=tracker.db

# API keys for data sources (optional)
# If NEWSAPI_KEY is missing the app uses Google News RSS instead.
NEWSAPI_KEY=your_newsapi_key
REDDIT_CLIENT_ID=your_reddit_client_id
REDDIT_CLIENT_SECRET=your_reddit_client_secret
REDDIT_USER_AGENT=your_app_name
# Facebook scraping uses public pages; no API key required.
# Instagram scraping uses public profiles; no API key required.

# Enable or disable data sources (1 = on, 0 = off)
ENABLE_TWITTER=1
ENABLE_REDDIT=1
ENABLE_NEWS=1
ENABLE_FACEBOOK=1
ENABLE_INSTAGRAM=1

# Email settings for daily digests (optional)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your_email@example.com
SMTP_PASSWORD=your_password

# Local LLM via Ollama (e.g. Qwen3 model). If omitted, a small Transformers
# model like t5-small is used for summaries.
OLLAMA_MODEL=qwen3
# OLLAMA_HOST=http://localhost:11434
